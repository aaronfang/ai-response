# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构。

## [0.2.0] - 2026-08-15

### Changed

- 快捷指令更名为“灵动回复”，图标改为大脑样式；
- 选择候选回复后只复制到剪贴板，不再自动打开微信或其他 App；
- 回复语言严格跟随聊天内容的主要语言，不受回复方向所用语言影响；
- 生成前增加可选的回复方向或大致内容输入，留空时继续使用默认提示词；
- 将用户填写的回复方向提升为全部候选回复必须遵循的内容约束，避免默认风格改变用户立场；
- 降低生成温度，减少模型忽略回复方向或虚构理由的情况；
- 候选回复由 5 条增加为 9 条；
- 第 1～3 条偏向高情商、幽默和情绪价值；
- 第 4～6 条偏向话少、高级幽默和绝不内耗；
- 第 7～9 条偏向精明得体，适合与长辈或领导交流；
- 增加模型输出余量，以容纳更多且差异更明显的候选回复。

## [0.1.0] - 2026-08-15

### Added

- 从 iOS 共享菜单接收选中的聊天文字；
- 没有共享输入时自动读取剪贴板；
- 没有剪贴板内容时允许手动输入；
- 调用 DeepSeek Chat Completions API；
- 使用 JSON Output 生成 5 条候选回复；
- 默认使用自然语气；
- 选择回复后自动复制到剪贴板；
- 复制完成后自动打开微信；
- API Key 配置、API 错误和原始响应诊断；
- Cherri 编译脚本和快捷指令结构检查脚本。

### Security

- 源码和 Release 资产不包含真实 DeepSeek API Key；
- `.gitignore` 忽略本地构建产物和系统文件。
